# 更新记录 (Changelog)

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式,
版本号遵循语义化版本。

## [0.2.2] - 2026-09-06

### 修正
- `/cancel` 只取消**当前会话**的回合;当前会话没有正在运行的回合时如实提示,
  不再波及其他项目的进程(0.2.1 的跨会话取消按反馈收窄)。`/cancel <id>`
  显式指定行为保留。

## [0.2.1] - 2026-09-06

### 调整
- `/cancel` 增加跨会话取消逻辑(在 0.2.2 中按用户要求收窄,仅保留当前会话)。

## [0.2.0] - 2026-09-06

### 修正
- **切换会话后看不到历史**:`sessionQuery.listEvents` 返回的是不含 `data`
  的轻量记录,消息文本与待办读取出来永远是空。历史摘要改用
  `readSurface`(折叠后的当前对话流,含完整载荷),待办改用
  `filterEvents(type=todo/write)`,旧路径保留为降级方案。

### 变更
- 审批/提问卡片、只读通知、历史摘要中的裸会话 ID(`session-uuid`)替换为
  **项目标签**(`sessionLabel`),标签来自 `/new` 任务描述与 `/open` 标题,
  持久化保存。

## [0.1.9] - 2026-09-06

### 修正
- **审批按钮被桌面 UI 抢答**:dsh 的审批 waterfall 是扁平监听表、按注册顺序
  执行,桌面客户端连接更早、永远先应答。agent 作用域监听注册改用
  `prepend(true)` 插队到最前,Telegram 按钮在会话被桌面打开时也能直接应答。

## [0.1.8] - 2026-09-06

### 新增
- 关键路径诊断日志(启动扫描数、监听安装、waterfall 命中),定位上一版
  监听未被咨询的问题。

## [0.1.7] - 2026-09-06

### 新增
- 通过 `agent/created` 事件与启动存活扫描,把审批/提问监听注册到每个
  agent 的**自身作用域**(`agent.ctx`),使插件进入审批 waterfall 的
  应答链(此前仅全局注册,会话在桌面打开时永远轮不到)。

## [0.1.6] - 2026-09-06

### 新增
- **审批双通道**:除交互卡片外,旁观 `approval/asked` 审计事件——请求被
  桌面持有(会话在桌面打开)时推送只读通知,`approval/decided` 到达后把
  结果(✅/⛔/⏹)同步回 TG 消息;按 callId 去重防止双卡。

## [0.1.5] - 2026-09-06

### 修正
- **`/new` 全部失败**:`agentPreset: default` 在宿主不存在(可用:
  standard/ptc/minimal/cordis)。新增 `resolveAgentPreset()`,配置的预设
  不存在时打警告并回退 `standard`;同步修正默认配置。

## [0.1.4] - 2026-09-06

### 修正
- **总根因**:`loadState` 要求 `chats` 为数组,而 `saveState` 写出的是对象
  ——每次重启状态文件被整体丢弃,所有会话绑定清零(审批推不到 TG、切换
  会话像重新开始)。改为按实际格式校验,兼容补齐 `sessions`/`tags`。

### 新增
- `/open` 切换会话后自动推送**现场恢复摘要**(状态 + 待办 + 最近 3 条消息)。
- 多项目**推送标签**:同一聊天链接多个会话时,事件推送带「项目名」前缀。

## [0.1.3] - 2026-09-06

### 新增
- `fallbackChat()`:未绑定聊天(如桌面端创建)的会话产生的审批/提问请求,
  转发到已授权聊天并标注全局转发提示;发送失败写 `logger.warn`,不再静默
  回退桌面。
- 全局转发的卡片附 `/open` 接管提示。

## [0.1.0] - 2026-09-03

### 首个发布
- Telegram 远程控制 DeepSeek Harness:远程建会话、实时进展推送、远程审批
  与问题应答、Todo 观察、SOCKS5/HTTP 代理支持。
